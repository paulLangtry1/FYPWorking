<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfyp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Quik Hire"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFYP">
        <activity android:name=".edit_contract_company"></activity>
        <activity android:name=".view_skills" />
        <activity android:name=".AdminHome" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".AdminRegister" />
        <activity android:name=".RecommendedJobs" />
        <activity android:name=".EnlargeReferenceforcompany" />
        <activity android:name=".EnlargedReference" />
        <activity android:name=".Company_view_previous_employee" />
        <activity android:name=".Company_rate_employee" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsSiteLocation"
            android:label="@string/title_activity_maps_site_location" />
        <activity android:name=".Approve_current_safepass" />
        <activity android:name=".approve_safe_pass" />
        <activity android:name=".Company_Approve_Disaprove_User" />
        <activity android:name=".Company_consider_contract" />
        <activity android:name=".company_edit_profile" />
        <activity android:name=".chatForum" />
        <activity android:name=".Edit_profile" />
        <activity android:name=".CurrentFeedback" />
        <activity android:name=".allReviews" />
        <activity android:name=".User_Feedback" />
        <activity android:name=".UserContractHistory" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".verifyActivity" />
        <activity android:name=".UserViewContracts" />
        <activity android:name=".CurrentContract" />
        <activity android:name=".ViewAllContracts" />
        <activity android:name=".AddContract" />
        <activity android:name=".CompanyHomeActivity" />
        <activity android:name=".CompanyRegister" />
        <activity android:name=".CompanyLogin" />
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>